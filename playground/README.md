# Experimental Playground Module

## 职责 (Responsibilities)
可扩展实验模块，用于存放微服务模板、响应式编程示例等。

## 技术栈 (Tech Stack)
- Java 21
- 可按子模块自定义

## 快速启动 (Quick Start)
```bash
mvn clean install -pl playground
```

## 扩展指南 (Extension Guide)
使用 `mvn archetype:generate` 创建新的子模块并加入 `playground/pom.xml`。
